#-1. Write a function that takes a number as an input and returns that number squared
def returnInputSquared(x):
    return x**2

#-2. Create a function that accepts a string as a parameter and prints it.
def printString(s):
    print(s)
    
#-3. Write a function that takes three required parameters and two optional parameters
def manyParaFunction(x,y,z, a = 1, b = 1):
    return x+y+z**(a+b)

#-4. Write a program with two functions. The first should take an integer as a parameter and return it divided by two. The second function should take a parameter and return it multiplied by four. Save the result of the first function to a variable. Call the second function using the variable assigned by the first
def fOne(x):
    return x/2
def fTwo(x):
    return x*4

pox = fOne(6)
fTwo(pox)

#-5. Write a function that converts a string to a float and returns it.

#-6. Add a docstring to all challenges.





#<-FUNCTION CALLS->